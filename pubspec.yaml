name: dart_source_gen
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/porum/dart_soruce_gen

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  source_gen:
  build_runner:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
